# Optional — enables the online AI coach. Without these, the app uses the
# built-in offline coach and writing/speaking analyzers (everything still works).
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Optional model overrides
# ANTHROPIC_MODEL=claude-sonnet-4-5
# OPENAI_MODEL=gpt-4o-mini
